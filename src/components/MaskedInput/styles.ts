import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 50,
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 10,
    borderColor: "#D3FA3A",
    borderWidth: 2,
    color: "#fff",
    textAlign: "right",
  },
});
