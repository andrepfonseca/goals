import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 50,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: "#CCCCCC",
    paddingHorizontal: 15,
    fontSize: 16,
    elevation: 2,
    marginBottom: 10,
    textAlign: "right",
  },
});
